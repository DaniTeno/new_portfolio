export class ProjectCard {
    constructor(title, msg, url, git, img){
        this.title = title;
        this.msg = msg;
        this.url = url;
        this.git = git;
        this.img = img;
    }
}